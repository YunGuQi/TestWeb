# 小红书爆款心理测评 H5 生成指南 (Master Prompt)

> **使用说明**：当需要开发新的测评小游戏时，请直接复制下方的**【通用指令模板】**并填入具体需求，发送给 AI 助手（如 Antigravity），AI 将严格按照此 SOP 自动完成构思、设计、开发与小红书运营策划。

---

## 🎯 【通用指令模板】(直接复制发给 AI)

请启动你的思考引擎，严格按照以下 5 个阶段，为我开发一个全新的小红书心理测评 H5 网页：

**【本次测试基本需求】**
- **测试主题**：[填写主题，例如：职场动物性格测试 / MBTI 恋爱匹配 / 精神防线测试]
- **目标受众**：[填写受众，例如：20-30岁职场打工人 / 备受情感内耗折磨的年轻人]
- **期望分享结果形态**：[填写结果载体，例如：一张打卡车票 / 一份诊断书 / 一张带有复古印章的档案卡]
- **设计风格偏好**：[填写风格，例如：极简冷淡风 (wabisabi) / 机械废土风 (industrial-brutalist-ui) / 赛博朋克风 (cyber)]

---

### 阶段一：创意构思与机制设计 (Brainstorming)
1. 请先启动`grill-me` `brainstorming` 或 `interview-me` 技能（如有需要），与我进行一轮需求对齐。
2. **题目设计**：根据具体的测试主题和受众耐心程度，分析并决定最合适的题目数量，设计极具代入感的沉浸式选择题。
3. **维度与算法**：根据测试的专业深度，灵活规划最合适的评价维度（具体分析再决定多少维度合适），并构建对应的加减分或权重映射逻辑。
4. **结果池 (Result Pool) 构建**：根据算法组合，设计合理数量的测试结果种类。每个结果需包含：类型名称、专属 Tag、一句极具共鸣的“扎心”或治愈文案。

### 阶段二：高级视觉与 UI 设计 (Design Taste)
1. 请调用 `design-taste-frontend` 或对应的风格技能（如 `industrial-brutalist-ui`）。
2. **严禁**使用看起来像廉价后台管理的通用 Bootstrap 组件。必须呈现小红书高级审美。
3. **视觉要求**：
   - 使用强排版对比（巨大的标题、微小的修饰性 Tag）。
   - 添加背景噪点（Noise Filter）或特殊纹理来增加质感。
   - 包含平滑的微动画（Micro-interactions）和过度效果。
   - 结果页必须像一个真实的物理凭证（如车票、收据、档案）。

### 阶段三：前端架构与核心开发 (Code Implementation)
1. **单文件架构**：不要搭建 React/Vue 脚手架，请使用纯粹的单文件 `index.html`，通过 CDN 引入 TailwindCSS 和所需的库。逻辑直接写在 `<script>` 中。
2. **组件流转**：包含“首页 (Landing)” -> “答题页 (Quiz)” -> “结果生成页 (Result)”。
3. **海报生成**：务必引入 `html2canvas.min.js`，实现长按/点击按钮将结果页 DOM 转化为高清图片，以便用户分享。
4. **历史记录**：利用浏览器的 `localStorage` 缓存测试历史记录，并提供查看入口。

### 阶段四：测试矩阵引流与置顶逻辑 (Cross-Promotion)
1. 在页面的底部结构中，预留 `<div id="test-matrix-container" class="w-full"></div>` 容器。
2. 确保在生成海报的主要按钮下方，引入公共的测试矩阵模块（`../common/test-matrix.js`）。
3. 确保此测试在矩阵列表中被正确识别，并**自动置顶在引流列表的第 01 位**，带上醒目的 `[当前]` 标签。
4. **纪律警告**：后续如需调整 HTML 结构，请务必直接使用编辑器工具（如 `replace_file_content`）修改，**绝对禁止使用 Python 脚本等间接手段篡改前端文件**。

### 阶段五：小红书发帖与传播物料策划 (Social Operation)
1. 前端代码开发并验证通过后，请自动调用 `xhs-social-content` 等相关技能。
2. 为刚刚生成的测试撰写 3 个极具爆款潜质的**小红书笔记标题/钩子 (Hook)**。
3. 提供一篇完整的小红书正文排版文案，带好适当的 Emoji 和话题 Tag（#心理测试 #周末去哪儿 #人格测试 等）。
4. （可选）为向微信私域或主页店铺引流提供话术建议。

---
> 收到指令后，请不要一次性输出所有代码。请先给出**阶段一（创意构思与维度设计）**的方案，等我确认后再进入后续的 UI 设计与代码开发。
